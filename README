A library for processing (processing.org) to render/control Animata (http://animata.kibu.hu) scenes.

Usage is something like:

AnimataP5 playback;
void setup(){
	 playback = new AnimataP5(this,"myscene.nmt");
}
void draw(){
	playback.draw(0,0); // draw at position (0,0)
}