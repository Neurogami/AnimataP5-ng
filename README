A library for processing (processing.org) to render/control Animata (http://animata.kibu.hu) scenes.

Usage will be something like:

AnimataPlayback playback;
void setup(){
	 playback = new AnimataPlayback(this);
	 playback.addScene("myscene.nmt");
}
void draw(){
	// drawn called manually
}